#!/usr/bin/env bash
# symbol-anchor-witness — a carrier that NAMES a symbol should name one the source still defines.
# The rung below source-pointer: that gate asks whether a meme points at a FILE, this asks whether the
# symbol it stands for still exists. A COUNTED debt against a ceiling that only shrinks, so the class
# stays visible without blocking on a canon sweep.
set -uo pipefail
cd "$(dirname "$0")/.."
REPO="$PWD" node tools/symbol-anchor.mjs
