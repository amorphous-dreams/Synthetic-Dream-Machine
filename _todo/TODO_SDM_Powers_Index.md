# TODO: SDM Powers Index Cleanup
`Synthetic_Dream_Machine/Synthetic_Dream_Machine_02_Traits_Index.md`
## Normalize Power Template Formatting
1. Update the Power template block to mirror the Traits schema style (consistent heading, `text:` blockquote formatting, `tags:` blockquote list, `meta:` blockquote list, and back-link block).
2. Ensure the template notes match current Powers conventions (Power, Range, Target, Duration, Overcharge, etc.) without introducing new rules text.
3. Keep the template as schema-only (no examples or rules content).

## Rebuild Powers Table of Contents
1. Regenerate the Powers TOC to mirror the Traits TOC layout (one bullet per entry, stable anchors, alphabetical order).
2. Remove any malformed or inline list formatting.
3. Confirm each TOC entry points to the correct `#sdm-power-<slug>` anchor.

## Clean Up Individual Power Entries
1. For every Power in `Synthetic_Dream_Machine/Synthetic_Dream_Machine_03_Powers_Index.md`, normalize formatting to the canonical template.
2. Convert any stray paragraphs or mixed formatting into the standard `text:` blockquote section.
3. Ensure `tags:` and `meta:` use blockquote lists; preserve existing data and sources.
4. Fill missing structural fields if present in the template but do not invent new rules text.
5. Keep ordering stable and avoid rewording unless needed for formatting consistency.
