/** Absolute path to the lares/ directory in the monorepo. */
export declare const laresRoot: string;
/** Absolute path to the monorepo root (two levels up from packages/lares-core/). */
export declare const repoRoot: string;
