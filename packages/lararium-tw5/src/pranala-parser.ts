/**
 * @deprecated web2-era — entirely dead. See pranala-parser.web2.ts for the original.
 * Rebuild targets:
 *   grammarRulesFromText → @lararium/core/meme-grammar (grammar IS a meme)
 *   parsePranalaEdges    → parseMemeEdges (@lararium/core/meme-ast)
 */
