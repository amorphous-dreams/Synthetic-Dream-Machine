/**
 * @deprecated web2-era — entirely dead. See carrier-write.web2.ts for the original.
 * Rebuild target: meme-write.ts (lararium-tw5/src/)
 *   exportCarrierText  → exportMemeText
 *   buildDirectRecord  → buildDirectRecord (same contract, different import)
 *   replaceMemeSlot, removeMemeSlot, composeMemeSlotBody → same names, new file
 */
