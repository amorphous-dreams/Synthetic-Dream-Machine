/**
 * @deprecated web2-era — entirely dead. See parser.web2.ts for the original.
 * Rebuild targets:
 *   parseMemeCarrier → parseMemeText    (@lararium/core/meme-ast)
 *   edgesFromAst     → edgesFromMemeAst (@lararium/core/meme-ast)
 *   buildAst         → buildMemeAst     (@lararium/core/meme-ast)
 *   collectEvents    → collectEvents    (@lararium/core/meme-ast)
 */
