/**
 * @deprecated web2-era — entirely dead. See sync-adaptor.web2.ts for the original.
 * Rebuild target: meme-sync-adaptor.ts (lararium-tw5/src/)
 *   LarariumCrdtSyncAdaptor → MemeSyncAdaptor
 *   imports meme-write.ts (not carrier-write.ts)
 *   imports parseMemeText (not splitCarrierToTiddlers)
 */
