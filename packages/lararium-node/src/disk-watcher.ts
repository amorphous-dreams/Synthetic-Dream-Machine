/**
 * @deprecated web2-era — entirely dead. See disk-watcher.web2.ts for the original.
 * Rebuild target: meme-disk-watcher.ts
 *   Replace splitCarrierToTiddlers → parseMemeText + splitMemeToTiddlers (deserializer).
 *   Echo guard (projector.writing.has(uri)) is FFZ-aligned — keep verbatim.
 */
